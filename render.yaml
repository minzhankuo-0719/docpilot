services:
  - type: web
    name: docpilot-mcp
    runtime: docker
    plan: free
    dockerfilePath: ./apps/mcp_server/Dockerfile
    dockerContext: .
    envVars:
      - key: HOST
        value: "0.0.0.0"
