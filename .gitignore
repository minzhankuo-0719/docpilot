# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/

# uv / venv
.venv/
venv/

# Environment
.env
.env.local
*.env

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Office lock files (e.g. ~$transformer_presentation.pptx while PowerPoint has it open)
~$*

# Test / coverage
.pytest_cache/
.coverage
htmlcov/
.ruff_cache/

# Processed data — ignore everything by default, allow-list the index files
# we explicitly want shipped with the repo (so Render's cold start is fast).
data/processed/*
!data/processed/bm25_corpus.pkl
!data/processed/bm25_index.pkl
!data/processed/chunks.jsonl
!data/processed/embedding_ids.json
!data/processed/embeddings.npy

# Claude Code local settings (entire dir is machine-specific)
.claude/

# Local demo notes (personal, not for repo)
DEMO.md

# Raw extraction demo (local exploration only)
scripts/demo_raw.py
